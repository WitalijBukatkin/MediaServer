server:
  port: 8100

security:
  oauth2:
    resource:
      userInfoUri: http://${AUTH_SERVICE_URI}:5000/oauth/current

spring:
  application:
    name: file-service

  cloud:
    config:
      uri: ${CONFIG_SERVICE_URI}:8888
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user